#include "adapters/memory/memory-stream-reader.hpp"
#include "stream-buffer.hpp"

namespace astralix {

void MemoryStreamReader::read(void *data, size_t size) {
  // m_buffer->read(data, size);
};

} // namespace astralix
